MCPTools Scaffold: cloudflare.search

Purpose: Placeholder for Cloudflare search/analytics MCP server/tool (logs, R2, KV, D1, analytics queries).
Status: scaffold only (no transport/runtime implementation yet)

Notes:
- Configure via env vars/config.yaml. Never commit secrets.
- Add an inactive servers.json entry when implementing.